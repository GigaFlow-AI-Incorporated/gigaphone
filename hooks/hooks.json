{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --project \"${CLAUDE_PLUGIN_ROOT}\" gigaphone detect --repo \"${CLAUDE_PROJECT_DIR}\" || true"
          }
        ]
      }
    ]
  }
}
